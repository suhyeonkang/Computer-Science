### 프로그램이란?

→ 개발자들이 짠 코드의 집합이 컴퓨터가 이해할 수 있는 코드들로 컴파일 된 것

### 프로세스

→ **실행되고 잇는 프로그램**을 뜻함
→ 프로그램을 실행하게 되었을 때를 프로세스가 생성 됨
→ **프로그램 실행 정보 + 프로그래머가 짠 코드 + 실행되면서 쓰이는 데이터 = 프로세스** 
→ 운영체제가 중재함 

 cf) 인터럽트
     → CPU 작업 중인 프로세스를 대기 상태로 두고 인터럽트로 메모리에 올라온 프로세스를 
         먼저 실행
     → 대기 상태의 순간이 매우 빠르기 때문에 유저는 프로그램이 멈춘다는 느낌을 받지 않음

### 프로그래밍 언어

→ 프로그램을 만들 때 쓰이는 언어 
→ **CPU는 0,1만 읽을 수 있기 때문에 우리가 영어로 짠 코드를 읽을 수 없음** 
    따라서 컴파일 과정이 필요함

    cf) 고급 언어(프로그래밍 언어) → 컴파일러 → 저급 언어(기계어) 
         → 다만, 반드시 컴파일 된 결과물이 기계어인 것은 아니다.

    cf) 바이트 코드
         → 컴파일된 결과물이 CPU에 해석되는 게 아니라, 컴퓨터 SW에서 해석되는 코드
             ex) Java, Python

     cf) 빌드
           → 소스 코드를 실행할 수 있는 소프트웨어 산출물로 만드는 행위
           → 마우스를 클릭하면 바로 실행되는 상태

       cf) IDE
            → Integrated Development Environmnet
            → 코딩을 하고 디버깅, 컴파일 등의 전반적인 작업을 제공해주는 프로그램
